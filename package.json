{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run deploy:pages",
    "deploy:pages": "npm run build && wrangler pages deploy ./dist --project-name rajpalace",
    "start": "vite"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "@hono/node-server": "^1.18.0"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@types/node": "^24.5.2",
    "vite": "^6.3.5"
  }
}